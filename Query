CREATE TABLE mri_triage_results (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  image_name TEXT NOT NULL,
  severity_rating DECIMAL(5,2),
  comment TEXT,
  image_data TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
